import Axis from './axis';

class AxisY extends Axis {
  constructor(...args) {
    super(...args);
    this.TODO = undefined;
  }

  render() {
    this.TODO = undefined;
  }
}

export default AxisY;
