import Axis from './axis';

class AxisX extends Axis {
  constructor(...args) {
    super(...args);
    this.TODO = undefined;
  }

  render() {
    this.TODO = undefined;
  }
}

export default AxisX;
